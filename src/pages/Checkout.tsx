/**
 * Checkout — placeholder halaman checkout per slug acara.
 * Dilindungi oleh RequireAuth — hanya dapat diakses oleh pengguna terautentikasi.
 * Implementasi lengkap akan diisi oleh task berikutnya.
 */
export default function Checkout() {
  return <div>Checkout</div>;
}
