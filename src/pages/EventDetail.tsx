/**
 * EventDetail — placeholder halaman detail acara per slug.
 * Implementasi lengkap akan diisi oleh task berikutnya.
 */
export default function EventDetail() {
  return <div>EventDetail</div>;
}
